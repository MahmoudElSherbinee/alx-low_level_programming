#include <stdio.h>
#include "main.h"

/**
 * add - prints 10 times the alphabet, in lowercase,
 *
 * @x: check the inputs
 *
 * @y: the seconed parameter
 *
 * Return: z the value of addition
*/

int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
